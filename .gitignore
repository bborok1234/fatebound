# ── 비공개 자료 (절대 공개 레포에 올리지 않음) ──────────────
# 기획 문서(00~21)·세부 수치·소스 데이터·프로토타입·스킬·dev 하니스
/design/

# ── Python / uv ──────────────────────────────────────────────
.venv/
game/.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── 빌드 / 배포 산출물 ───────────────────────────────────────
dist/
build/
*.spec
*.egg-info/

# ── 개발 아티팩트 ────────────────────────────────────────────
*.svg
.omc/
.DS_Store

# ── Claude Code: 로컬 상태/설정은 제외, 공유 커맨드(/release 등)는 버전 관리 ──
.claude/*
!.claude/commands/
.claude/settings.local.json

# ── 세이브(로컬) ─────────────────────────────────────────────
*.save.json
